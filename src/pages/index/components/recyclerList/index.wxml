<view>
    <block wx:for="{{currentItems}}" wx:key="recyclerKey">
        <view class="renderItem">
            {{item.title}}
        </view>
    </block>
</view>
<view>
    <block wx:for="{{regions}}" wx:key="index">
        <view></view>
        <view></view>
    </block>
</view>